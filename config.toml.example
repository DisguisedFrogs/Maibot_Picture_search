# 插件配置模板：复制为 config.toml 后按需修改。
# 未提供 config.toml 时 MaiBot 按插件代码默认值自动生成配置，无需手动创建。
[plugin]
enabled = true
config_version = "1.4.0"

[mood]
window = 20
min_new_messages = 3
willingness_alpha = 0.5
level_confirm_times = 2
model = "replyer"
max_keywords = 5
pixiv_db_path = ""
